pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pytest>=7.0

# Stretch goal from the Week 8 proposal (not required to reproduce the
# Week 10 baseline numbers, which use random_forest as the tree-ensemble
# entry instead -- see docs/data_access_and_versioning.md):
# xgboost>=2.0
# shap>=0.44
